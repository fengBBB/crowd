server:
  port: 4000
spring:
  application:
    name: feng-crowd-auth
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  redis:
    host: localhost
  session:
    store-type: redis


eureka:
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka

shortmessage:
  host: https://dfsns.market.alicloudapi.com
  path: /data/send_sms
  method: POST
  appcode:

ribbon:
  ReadTimeout: 10000
  ConnectTimeout: 10000