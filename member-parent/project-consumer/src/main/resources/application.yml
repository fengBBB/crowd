server:
  port: 6000
spring:
  application:
    name: feng-crowd-project
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
  redis:
    host: localhost
  session:
    store-type: redis

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka

aliyun:
  oss:
    access-key-id:
    access-key-secret:
    bucket-domain:
    bucket-name: feng2021
    end-point: http://oss-cn-shenzhen.aliyuncs.com

ribbon:
  ReadTimeout: 10000
  ConnectTimeout: 10000